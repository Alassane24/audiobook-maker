# Python
.venv/
.venv-clone/
.venv-f5/
__pycache__/
*.pyc

# Tier-2 voice cloning: reference clips (copyrighted) + setup log
voice-refs/
clone-setup.log

# Frontend (frontend/.gitignore covers node_modules/.next; out/ IS committed)

# Per-run job artifacts & generated audio (large, regenerated each run)
web/jobs/
*.m4b
*.m4a

# CLI scratch / generated output dirs (regenerated on demand)
rezero-v23/

# scratch / temp
.testjob
test_*.wav
test_*.m4a
sample.mp3

# stray sample/test artifacts (keep only samples/voices/ — the preview clips)
samples/out/
samples/*.jpg

# OS
Thumbs.db
.DS_Store
